# 日志配置
[log]
level = DEBUG
# DEBUG, INFO, WARNING, ERROR, CRITICAL
file = logs/akshare_fetcher.log
max_bytes = 10485760
# 10MB
backup_count = 5
console = true
# 是否输出到控制台

[refresh]
trading_interval = 900000
# 交易时间刷新间隔(毫秒), 默认15分钟
non_trading_interval = 0
# 非交易时间刷新间隔(毫秒), 0表示不刷新